import Dashboard from 'ui/pages/Dashboard/Dashboard';
import Login from '../ui/pages/Login/Login';

export default {
    ROUTES: [
        {
            path: '/',
            component: Login
        }
    ]
}